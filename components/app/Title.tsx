import Typography from "@mui/material/Typography";

const Title: React.VFC = () => {
  return (
    <Typography variant="h4" sx={{ color: "#3F1052", fontFamily: "Arial Black" }}>
      PremireLeagueResult
    </Typography>
  );
};

export default Title;
